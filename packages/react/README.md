# `@rosettadash/react`

RosettaDash React runtime. Import paths match `@rosettadash/web-components`.

```ts
import { Accordion, type AccordionProps } from '@rosettadash/react/layout/accordion';
```

Peer dependency: `react` >= 18.
