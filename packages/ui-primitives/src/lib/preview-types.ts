export interface PreviewRow {
  id: string;
  name: string;
  status: string;
  amount: number;
  date: string;
}

export interface PreviewSelectOption {
  label: string;
  value: string;
}

export interface PreviewChartPoint {
  label: string;
  value: number;
}
