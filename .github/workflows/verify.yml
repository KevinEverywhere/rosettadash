name: Verify

on:
  pull_request:
    branches: [development]
  push:
    branches: [development]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install npm 11 (lockfile requires npm 11+)
        run: npm install -g npm@11.17.0

      - name: Install dependencies
        run: |
          npm --version
          npm ci

      - name: Lint, typecheck, and unit tests
        run: npm run verify

  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install npm 11 (lockfile requires npm 11+)
        run: npm install -g npm@11.17.0

      - name: Install dependencies
        run: |
          npm --version
          npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: E2E smoke tests
        run: npm run e2e
